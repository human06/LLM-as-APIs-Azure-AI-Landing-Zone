<policies>
    <inbound>
        <set-backend-service id="apim-generated-policy" backend-id="aoai-2" />
        <azure-openai-token-limit tokens-per-minute="1000000" counter-key="@(context.Subscription.Id)" estimate-prompt-tokens="true" tokens-consumed-header-name="consumed-tokens" remaining-tokens-header-name="remaining-tokens" />
        <authentication-managed-identity resource="https://cognitiveservices.azure.com" output-token-variable-name="msi-access-token" client-id="{{apim-identity}}" ignore-error="false" />
        <set-header name="Authorization" exists-action="override">
            <value>@("Bearer " + (string)context.Variables["msi-access-token"])</value>
        </set-header>
        <azure-openai-emit-token-metric namespace="genaitest">
            <dimension name="Subscription ID" />
            <dimension name="Client IP" value="@(context.Request.IpAddress)" />
        </azure-openai-emit-token-metric>
        <base />
    </inbound>
    <backend>
        <retry condition="@(context.Response.StatusCode == 429 || context.Response.StatusCode == 404 || context.Response.StatusCode == 503)" count="1" interval="1" first-fast-retry="true">
            <choose>
                <when condition="@(context.Variables.GetValueOrDefault<int>("retryCount", 0) == 0)">
                    <forward-request />
                </when>
                <otherwise>
                    <set-backend-service backend-id="aoai-lb-pool" />
                    <forward-request />
                </otherwise>
            </choose>
        </retry>
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <set-variable name="retryCount" value="@(context.Variables.GetValueOrDefault<int>("retryCount", 0) + 1)" />
        <base />
    </on-error>
</policies>